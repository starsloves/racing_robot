---
description: Stage2 参数测试 — 世界坐标唯一真值（必读，禁止回退链式/边长/原点折线）
globs: src/racing/racing_stage2_param_test/**/*
alwaysApply: true
---

# Stage2 参数测试 — 世界坐标（强制）

## 唯一坐标系

- 全程 **一套 map / `/odom_combined` 世界坐标** `(x, y, yaw)`（与 `inertial_stage2.yaml` `corridor_goal` 同系）。
- **禁止**另建：每段 P0 归零、SIM_CHAIN、reanchor、局部 (0,0) 链式折线、`rectangle_*_leg_m` launch 边长当真值。

## 唯一路点真值

- 文件：`src/racing/racing_stage2_param_test/config/field_track_clockwise.yaml`（CCW 用 `field_track_counterclockwise.yaml`）。
- Launch：`field_track_config` 为空则按 `test_direction` 自动选上述文件。
- 每段 move 只认 yaml 里的 **`start` / `end` / `heading_deg`（ψ）**。
- **Stage2 入口**：`channel_entry.xy` = **map (2.50, 3.20)**，`yaw_deg: 90.0`（与官方 `corridor_waypoints_json` 一致）。

## channel_entry 与第一段

| 点 | 含义 |
|----|------|
| `channel_entry` **(2.50, 3.20)** | Stage1 结束 / `rect_enter_align` 参考 |
| `rect_first_leg.start` **(2.90, 3.57)** | 第一段直行 **S**（≠ channel_entry） |

**禁止**把 channel_entry 当作 `rect_first_leg` 的 S。

## ψ（heading_deg）

- 直行段 ψ **仅**用正交角：**90 / 0 / -90 / 180**（车头沿矩形边）。
- ψ = 该段行驶方向；S→E 必须在 ψ 轴上。
- **禁止**把 IMU 场测杂角（89°、4°、105° 等）写进 plan ψ。

## 边长怎么来

- **不由 launch 填写**。
- 程序计算：`L = (E−S)·(cos ψ, sin ψ)`（`field_track.py`）。
- plan 里 `distance_m` 只是上述 L 的副本，**真值仍是 yaml 的 S/E/ψ**。

## 转向（`angle_deg>0` = 左转）

- **channel_entry 名义航向 = 90°（+Y）**，yaml `channel_entry.yaw_deg: 90.0`。
- **`rect_enter_align`**：由扫码方向决定 — 顺时针 **+90°**，逆时针 **−90°**（`field_entry_turn_deg`）。
- 四角：顺时针 **−90°**；逆时针 **+90°**（`field_corner_turn_deg`）。

## 控制

- 无障直行：**target = 段末 E**；沿程/横偏用 **S + ψ**。
- 段完成：近 **E**（≤~0.12 m）+ 横偏小；整圈 finish = `mission_finish_segment` 的 E。
- 避障：仍在同一段 S→E 上 handoff，见 `docs/AVOIDANCE.md`。

## 改赛道

只改 `config/field_track_*.yaml` 的角点与 ψ；改完 `colcon build --packages-select racing_stage2_param_test` 验证。
